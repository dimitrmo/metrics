services:

  ws-server-go:
    build:
      dockerfile: Dockerfile
      context: ws-server-go
    ports:
      - "8080:8080"

  ws-server-rs:
    build:
      dockerfile: Dockerfile
      context: ws-server-rs
    ports:
      - "8081:8081"
